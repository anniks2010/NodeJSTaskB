<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">

    <title>Estonia COVID Stats</title>
</head>
<body>
  <% if(corona){%>
    <div class="container" >
        <nav class="navbar navbar-expand-lg navbar-light bg-light" >
            <a class="navbar-brand" href="#"><%= corona.countrytimelinedata[0].info.title%></a>
        </nav>
        <div class="row" style="margin: 0;">
            <table class="table">
                <thead>
                  <tr>
                    <th scope="col">#</th>
                    <th scope="col"><%= Object.keys(dates)[0]%></th>
                    <th scope="col"><%= Object.keys(dates)[Object.keys(dates).length-2]%></th>
                  </tr>
                </thead>
                <tbody>
                  <tr>
                    <th scope="row">New daily cases</th>
                    <td><%= dates[Object.keys(dates)[0]].new_daily_cases%></td>
                    <td><%= dates[Object.keys(dates)[Object.keys(dates).length-2]].new_daily_cases%></td>
                  </tr>
                  <tr>
                    <th scope="row">Total cases</th>
                    <td><%= dates[Object.keys(dates)[0]].total_cases%></td>
                    <td><%= dates[Object.keys(dates)[Object.keys(dates).length-2]].total_cases%></td>
                  </tr>
                  <tr>
                    <th scope="row">Total recoveries</th>
                    <td><%= dates[Object.keys(dates)[0]].total_recoveries%></td>
                    <td><%= dates[Object.keys(dates)[Object.keys(dates).length-2]].total_recoveries%></td>
                  </tr>
                </tbody>
              </table>
        </div>
    </div>

    
    
   <%}%>
     
</body>
</html>
